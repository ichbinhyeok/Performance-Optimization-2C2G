📊 1. 로깅 문제 (9.8%)

**프로파일 메소드**

```
ch.qos.logback.classic.spi.TurboFilterList.getTurboFilterChainDecision - 94,697 ms

```

**병목 예측**

1. DEBUG 로깅으로 인한 과도한 로그 생성
2. `getTurboFilterChainDecision` 메소드가 모든 로그에 대해 호출됨
3. DEBUG 레벨에서는 로그를 출력하지 않더라도 필터 체인을 거쳐야 함

**해결 방안**

- 로그 레벨 상향 조정 (DEBUG → INFO)
- 운영 환경별 로그 설정 분리